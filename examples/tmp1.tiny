int a;
int b;
char c;

a := 1;

if (a) then
  a := 2;
  if (b) then
    b := 3;
  else 
    a := a + 1;
  end
else
  if (c > 0) then
    c := 1;
  end
  write(1);
end

repeat
  a := a >= c;
  b := b;
until (c >= (1 + 1 / 2 - 1))
